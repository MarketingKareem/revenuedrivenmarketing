---
title: 'Example post title (this is a template, not published)'
description: 'One-sentence summary shown on the blog index and in search results, kept under 155 characters.'
pubDate: 2026-01-01
pillar: general
draft: true
---

This file is a template for the content pipeline, not a real post. `draft: true` keeps it off the live site.

Frontmatter fields:

- `title`: page title and blog index heading
- `description`: meta description / index teaser
- `pubDate`: publish date, `YYYY-MM-DD`
- `pillar`: one of `google-ads`, `meta-ads`, `attribution`, `case-study`, `general`
- `draft`: `true` hides the post; flip to `false` (or remove) to publish

Body content below the frontmatter is plain Markdown.
